# New Index Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rewrite `index.html` with an interactive entry overlay (need-assessment quiz) and modernized content sections inspired by `aurobase-real.html`, keeping the colorlib sidebar and white + orange brand identity.

**Architecture:** Single-file rewrite of `index.html`. The `<head>` (meta/SEO/JSON-LD), GTM, float button, modal form, sidebar, loader, and JS script tags are preserved unchanged. The scoped `<style>` block and `#colorlib-main` content are fully rewritten. An entry overlay with `position: fixed` covers `#colorlib-main` on first visit.

**Tech Stack:** HTML5, CSS3 (scoped `<style>`), vanilla JS, jQuery 3.2.1 (already loaded), Bootstrap 4 grid (existing), LINE Seed TW font (existing), Flaticon/Ionicons (existing).

**Spec:** `docs/superpowers/specs/2026-04-13-new-index-design.md`

**Reference files:**
- `aurobase-real.html` — layout/content source (adapt from dark+red to white+orange)
- `aurobase-entry.html` — entry overlay interaction source
- Current `index.html` — preserved structure (head, sidebar, modal, scripts)

---

## File Map

Only one file is modified:

| File | Action | Responsibility |
|------|--------|----------------|
| `index.html` | Rewrite | Replace scoped `<style>` (lines 84-405) and `#colorlib-main` content (lines 774-1111). Preserve everything else. |

No new files created. No changes to `css/style.css`, other HTML pages, or JS files.

---

## Task 1: Back up current index and scaffold new structure

**Files:**
- Modify: `index.html`

- [ ] **Step 1: Create a backup of the current index.html**

```bash
cp index.html index.html.bak
```

- [ ] **Step 2: Replace the scoped `<style>` block (lines 84-405) with a new empty style block**

Replace everything from `<style>` (line 84) through `</style>` (line 405) with:

```html
<style>
/* ══════════════════════════════════════════════
   NEW INDEX PAGE — Scoped Styles
   White + Orange (#F96D00) brand system
   ══════════════════════════════════════════════ */

/* placeholder — filled in Task 2 */
</style>
```

- [ ] **Step 3: Replace `#colorlib-main` content (lines 774-1111) with section scaffolding**

Replace everything from `<div id="colorlib-main">` (line 774) through the closing `</footer>` (line 1110) with:

```html
<div id="colorlib-main">

  <!-- ╔══ ENTRY OVERLAY ══╗ -->
  <div id="entryOverlay">
    <!-- filled in Task 3 -->
  </div>

  <!-- ╔══ HOMEPAGE CONTENT ══╗ -->

  <!-- Section 1: Hero -->
  <section id="idx-hero"><!-- filled in Task 4 --></section>

  <!-- Section 2: Ticker -->
  <div id="idx-ticker"><!-- filled in Task 4 --></div>

  <!-- Section 3: Core Services -->
  <section id="idx-services"><!-- filled in Task 5 --></section>

  <!-- Section 4: Solutions Grid -->
  <section id="idx-solutions"><!-- filled in Task 5 --></section>

  <!-- Section 5: Marquee -->
  <div id="idx-marquee"><!-- filled in Task 6 --></div>

  <!-- Section 6: Partners -->
  <div id="idx-partners"><!-- filled in Task 6 --></div>

  <!-- Section 7: CTA -->
  <section id="idx-cta"><!-- filled in Task 6 --></section>

  <!-- Section 8: FAQ -->
  <section id="idx-faq"><!-- filled in Task 7 --></section>

  <!-- Section 9: Footer -->
  <footer id="footer-sec" class="ftco-footer ftco-bg-dark ftco-section">
    <div class="container px-md-4">
      <div class="footer-legal text-center">
        <div class="container">
          <div class="row justify-content-center">
            <div class="copyright" style="color:#a9a9a9">
              Copyright &copy; 2023 奧洛股份有限公司, All Right Reserved. | 台北市內湖區洲子街101號2樓 | +886-2-5568-4688 | ad@aurocore.com | <a href="privacypolicy.html">隱私權政策</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </footer>

  <!-- Re-trigger button (hidden when overlay is visible) -->
  <button type="button" id="entryRetrigger" class="entry-retrigger" style="display:none;" onclick="openEntryOverlay()">
    <i class="icon-search"></i> 需求診斷
  </button>

</div><!-- END COLORLIB-MAIN -->
```

- [ ] **Step 4: Verify the page loads without JS errors**

```bash
python3 -m http.server 8000 &
# Open http://localhost:8000/index.html in browser
# Verify: sidebar renders, page loads, no console errors
# The main content area will be mostly empty — that's expected
```

- [ ] **Step 5: Commit**

```bash
git add index.html
git commit -m "refactor(index): scaffold new page structure with section placeholders"
```

---

## Task 2: Write all scoped CSS styles

**Files:**
- Modify: `index.html` (the `<style>` block)

- [ ] **Step 1: Write Entry Overlay styles**

Replace the `/* placeholder */` comment in the `<style>` block with the full CSS. Entry overlay styles:

```css
/* ── ENTRY OVERLAY ── */
#entryOverlay {
  position: fixed;
  top: 0; bottom: 0; right: 0;
  left: 340px; /* sidebar width */
  z-index: 900;
  background: #fff;
  display: flex; flex-direction: column;
  align-items: center; justify-content: center;
  transition: opacity 0.6s ease, transform 0.6s ease;
  overflow: hidden;
}
#entryOverlay.closing {
  opacity: 0;
  transform: scale(1.02);
  pointer-events: none;
}
#entryOverlay.closed { display: none; }

.entry-skip {
  position: absolute; top: 24px; right: 32px;
  font-size: 0.75rem; letter-spacing: 0.1em; text-transform: uppercase;
  color: #999; background: none; border: none; cursor: pointer;
  transition: color 0.2s;
}
.entry-skip:hover { color: #F96D00; }

/* Boot Screen */
#bootScreen {
  display: flex; flex-direction: column; align-items: center;
  width: 100%; max-width: 480px; padding: 0 32px;
}
.boot-logo {
  font-size: 3rem; font-weight: 700; color: #28282c; line-height: 1;
  margin-bottom: 4px; text-align: center;
}
.boot-logo span { color: #F96D00; }
.boot-tagline {
  font-size: 0.7rem; letter-spacing: 0.15em; color: #999;
  text-transform: uppercase; margin-bottom: 40px; text-align: center;
}
.boot-lines { width: 100%; display: flex; flex-direction: column; gap: 6px; margin-bottom: 24px; }
.boot-line {
  font-size: 0.72rem; letter-spacing: 0.04em; color: #ccc;
  display: flex; gap: 10px; align-items: center;
  opacity: 0; transform: translateX(-6px);
  transition: opacity 0.3s ease, transform 0.3s ease, color 0.3s ease;
}
.boot-line.visible { opacity: 1; transform: none; }
.boot-line.done { color: #999; }
.boot-line em { color: #F96D00; font-style: normal; }
.boot-bar-wrap {
  width: 100%; height: 2px; background: rgba(0,0,0,0.06);
  margin-bottom: 16px; overflow: hidden;
}
.boot-bar { height: 100%; width: 0%; background: #F96D00; transition: width 0.1s linear; }
.boot-pct {
  font-size: 0.68rem; letter-spacing: 0.1em; color: #999;
  text-align: right; margin-bottom: 28px;
}
.boot-cta {
  font-size: 0.78rem; letter-spacing: 0.1em; text-transform: uppercase;
  color: #fff; background: #F96D00; border: none;
  padding: 12px 36px; cursor: pointer;
  opacity: 0; pointer-events: none;
  transition: opacity 0.4s ease, background 0.2s;
}
.boot-cta.ready { opacity: 1; pointer-events: all; }
.boot-cta:hover { background: #e06000; }

/* Quiz Screen */
#quizScreen { display: none; flex-direction: column; align-items: center; width: 100%; max-width: 600px; padding: 0 32px; }
#quizScreen.active { display: flex; }

.q-progress { display: flex; gap: 8px; margin-bottom: 28px; }
.q-dot { width: 28px; height: 2px; background: rgba(0,0,0,0.08); transition: background 0.3s; }
.q-dot.done { background: rgba(249,109,0,0.4); }
.q-dot.active { background: #F96D00; }

.q-step {
  font-size: 0.68rem; letter-spacing: 0.15em; color: #F96D00;
  text-transform: uppercase; margin-bottom: 12px; text-align: center;
}
.q-title {
  font-size: 1.8rem; font-weight: 700; color: #28282c;
  text-align: center; line-height: 1.2; margin-bottom: 6px;
}
.q-sub {
  font-size: 0.72rem; color: #999; letter-spacing: 0.04em;
  text-align: center; margin-bottom: 32px; line-height: 1.6;
}
.q-opts { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; width: 100%; margin-bottom: 24px; }
.q-opt {
  background: #fff; border: 1px solid rgba(0,0,0,0.08);
  padding: 16px 18px; cursor: pointer;
  transition: border-color 0.2s, background 0.2s;
  position: relative; overflow: hidden; text-align: left;
  animation: qOptIn 0.3s ease forwards; opacity: 0;
}
@keyframes qOptIn { to { opacity: 1; transform: none; } from { opacity: 0; transform: translateY(6px); } }
.q-opt:nth-child(1) { animation-delay: 0.05s; }
.q-opt:nth-child(2) { animation-delay: 0.10s; }
.q-opt:nth-child(3) { animation-delay: 0.15s; }
.q-opt:nth-child(4) { animation-delay: 0.20s; }
.q-opt:hover { border-color: rgba(249,109,0,0.4); background: rgba(249,109,0,0.03); }
.q-opt.selected { border-color: #F96D00; background: rgba(249,109,0,0.06); }
.q-opt.selected::before {
  content: ''; position: absolute; left: 0; top: 0; bottom: 0;
  width: 3px; background: #F96D00;
}
.q-opt-code { font-size: 0.6rem; letter-spacing: 0.12em; color: #F96D00; text-transform: uppercase; margin-bottom: 4px; }
.q-opt-label { font-size: 1.1rem; font-weight: 600; color: #28282c; margin-bottom: 3px; }
.q-opt-desc { font-size: 0.72rem; color: #999; line-height: 1.5; }
.q-next {
  font-size: 0.75rem; letter-spacing: 0.1em; text-transform: uppercase;
  color: #fff; background: #F96D00; border: none;
  padding: 12px 36px; cursor: pointer;
  opacity: 0; pointer-events: none;
  transition: opacity 0.25s, background 0.2s;
}
.q-next.ready { opacity: 1; pointer-events: all; }
.q-next:hover { background: #e06000; }

/* Map/Result Screen */
#mapScreen { display: none; flex-direction: column; align-items: center; width: 100%; max-width: 600px; padding: 0 32px; }
#mapScreen.active { display: flex; }

.map-eyebrow { font-size: 0.68rem; letter-spacing: 0.15em; color: #F96D00; text-transform: uppercase; margin-bottom: 8px; }
.map-title { font-size: 1.6rem; font-weight: 700; color: #28282c; line-height: 1.2; margin-bottom: 24px; text-align: center; }

.territory {
  width: 100%; border: 1px solid rgba(0,0,0,0.08); padding: 18px 22px;
  margin-bottom: 10px; position: relative; overflow: hidden;
  transition: border-color 0.3s, background 0.3s;
}
.territory.recommended { border-color: #F96D00; background: rgba(249,109,0,0.04); }
.territory.recommended::before {
  content: ''; position: absolute; left: 0; top: 0; bottom: 0;
  width: 3px; background: #F96D00;
}
.t-flex { display: flex; align-items: center; justify-content: space-between; }
.t-code { font-size: 0.6rem; letter-spacing: 0.1em; color: #999; text-transform: uppercase; margin-bottom: 3px; }
.t-name { font-size: 1.1rem; font-weight: 600; color: #28282c; }
.t-sub { font-size: 0.72rem; color: #999; margin-top: 2px; }
.t-tag { font-size: 0.6rem; color: #F96D00; border: 1px solid rgba(249,109,0,0.35); padding: 2px 8px; letter-spacing: 0.08em; }
.rec-badge {
  display: none; position: absolute; top: 10px; right: 12px;
  font-size: 0.55rem; letter-spacing: 0.1em;
  color: #F96D00; border: 1px solid rgba(249,109,0,0.4);
  padding: 2px 7px; text-transform: uppercase;
}
.territory.recommended .rec-badge { display: block; }

.map-actions { display: flex; gap: 12px; margin-top: 20px; align-items: center; }
.map-enter {
  font-size: 0.75rem; letter-spacing: 0.1em; text-transform: uppercase;
  color: #fff; background: #F96D00; border: none;
  padding: 12px 28px; cursor: pointer; transition: background 0.2s;
}
.map-enter:hover { background: #e06000; }
.map-reset {
  font-size: 0.68rem; letter-spacing: 0.08em; color: #999;
  text-transform: uppercase; border: none; background: none;
  cursor: pointer; transition: color 0.2s;
}
.map-reset:hover { color: #F96D00; }
.map-home {
  font-size: 0.68rem; letter-spacing: 0.08em; color: #999;
  text-transform: uppercase; border: none; background: none;
  cursor: pointer; transition: color 0.2s;
}
.map-home:hover { color: #28282c; }

/* Re-trigger button */
.entry-retrigger {
  position: fixed; bottom: 90px; right: 24px; z-index: 800;
  background: #fff; border: 1px solid rgba(0,0,0,0.1);
  padding: 10px 16px; font-size: 0.72rem; color: #28282c;
  cursor: pointer; box-shadow: 0 2px 8px rgba(0,0,0,0.08);
  transition: border-color 0.2s, box-shadow 0.2s;
}
.entry-retrigger:hover { border-color: #F96D00; box-shadow: 0 4px 16px rgba(249,109,0,0.12); }
.entry-retrigger i { margin-right: 6px; color: #F96D00; }
```

- [ ] **Step 2: Write Hero section styles**

Append to the same `<style>` block:

```css
/* ── HERO ── */
#idx-hero {
  padding: 80px 48px 60px;
  display: grid; grid-template-columns: 55% 45%;
  gap: 32px; align-items: center;
}
.hero-left { display: flex; flex-direction: column; justify-content: center; }
.hero-eyebrow {
  font-size: 0.72rem; letter-spacing: 0.15em; color: #F96D00;
  text-transform: uppercase; margin-bottom: 20px;
  display: flex; align-items: center; gap: 10px;
}
.hero-eyebrow::before { content: ''; display: block; width: 20px; height: 1px; background: #F96D00; }
.hero-title {
  font-size: clamp(2.4rem, 4vw, 3.6rem); font-weight: 700;
  line-height: 1.1; color: #28282c; margin-bottom: 24px;
}
.hero-title .orange { color: #F96D00; }
.hero-desc { font-size: 0.9rem; color: #666; line-height: 1.8; max-width: 400px; margin-bottom: 36px; }
.hero-actions { display: flex; align-items: center; gap: 20px; }
.btn-primary-idx {
  font-size: 0.78rem; letter-spacing: 0.08em; text-transform: uppercase;
  color: #fff; background: #F96D00; padding: 12px 28px;
  text-decoration: none; display: inline-block; border: none;
  transition: background 0.2s, transform 0.15s;
}
.btn-primary-idx:hover { background: #e06000; transform: translateY(-1px); color: #fff; text-decoration: none; }
.btn-ghost-idx {
  font-size: 0.78rem; letter-spacing: 0.08em; text-transform: uppercase;
  color: #999; text-decoration: none; display: flex; align-items: center; gap: 6px;
  transition: color 0.2s;
}
.btn-ghost-idx:hover { color: #F96D00; text-decoration: none; }

.hero-right { border-left: 1px solid rgba(0,0,0,0.06); padding-left: 32px; }
.hero-map { display: flex; flex-direction: column; gap: 8px; }
.hero-map-label {
  font-size: 0.68rem; letter-spacing: 0.12em; color: #999;
  text-transform: uppercase; margin-bottom: 12px;
}
.hero-svc {
  border: 1px solid rgba(0,0,0,0.06); padding: 16px 20px;
  cursor: pointer; transition: border-color 0.2s, background 0.2s;
  position: relative; overflow: hidden; text-decoration: none; display: block; color: inherit;
}
.hero-svc:hover { border-color: rgba(249,109,0,0.35); background: rgba(249,109,0,0.03); color: inherit; text-decoration: none; }
.hero-svc::before {
  content: ''; position: absolute; left: 0; top: 0; bottom: 0;
  width: 3px; background: #F96D00;
  transform: scaleY(0); transition: transform 0.2s; transform-origin: bottom;
}
.hero-svc:hover::before { transform: scaleY(1); }
.hero-svc-code { font-size: 0.6rem; letter-spacing: 0.1em; color: #999; text-transform: uppercase; margin-bottom: 3px; }
.hero-svc-name { font-size: 1.1rem; font-weight: 600; color: #28282c; }
.hero-svc-sub { font-size: 0.7rem; color: #999; margin-top: 2px; }
.hero-svc-tag { font-size: 0.58rem; color: #F96D00; border: 1px solid rgba(249,109,0,0.35); padding: 2px 7px; letter-spacing: 0.08em; }
.hero-svc-row { display: flex; align-items: center; justify-content: space-between; }
.hero-sub-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; }
```

- [ ] **Step 3: Write Ticker + Core Services + Solutions styles**

Append:

```css
/* ── TICKER ── */
#idx-ticker {
  border-top: 1px solid rgba(0,0,0,0.06); border-bottom: 1px solid rgba(0,0,0,0.06);
  padding: 10px 0; overflow: hidden; white-space: nowrap; background: #f8f8f8;
}
.ticker-inner { display: inline-flex; animation: idxTicker 35s linear infinite; }
.ticker-item {
  font-size: 0.72rem; letter-spacing: 0.1em; color: #666;
  text-transform: uppercase; padding: 0 40px;
}
.ticker-item span { color: #F96D00; margin-right: 8px; }
@keyframes idxTicker { from { transform: translateX(0); } to { transform: translateX(-50%); } }

/* ── SECTIONS (shared) ── */
.idx-section { padding: 80px 48px; }
.idx-section-header {
  display: flex; align-items: flex-end; justify-content: space-between;
  margin-bottom: 48px; padding-bottom: 20px;
  border-bottom: 1px solid rgba(0,0,0,0.06);
}
.idx-section-num { font-size: 0.68rem; color: #F96D00; letter-spacing: 0.15em; margin-bottom: 8px; }
.idx-section-title { font-size: clamp(1.8rem, 3vw, 2.8rem); font-weight: 700; line-height: 1.1; color: #28282c; }
.idx-section-link {
  font-size: 0.72rem; color: #999; text-decoration: none; letter-spacing: 0.08em;
  text-transform: uppercase; display: flex; align-items: center; gap: 6px;
  transition: color 0.2s; white-space: nowrap;
}
.idx-section-link:hover { color: #F96D00; text-decoration: none; }

/* ── CORE SERVICES ── */
.srv-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1px; background: rgba(0,0,0,0.06); }
.srv-card { background: #fff; padding: 36px; transition: background 0.2s; }
.srv-card:hover { background: #fafafa; }
.srv-card.featured { grid-column: 1 / -1; display: grid; grid-template-columns: 1fr 1fr; gap: 36px; }
.srv-idx { font-size: 0.6rem; color: rgba(0,0,0,0.12); letter-spacing: 0.12em; margin-bottom: 20px; }
.srv-name { font-size: 1.8rem; font-weight: 700; color: #28282c; line-height: 1; margin-bottom: 6px; }
.srv-subtitle { font-size: 0.68rem; color: #F96D00; letter-spacing: 0.1em; text-transform: uppercase; margin-bottom: 16px; }
.srv-desc { font-size: 0.85rem; color: #666; line-height: 1.8; margin-bottom: 20px; }
.srv-tags { display: flex; flex-wrap: wrap; gap: 6px; }
.srv-tag {
  font-size: 0.62rem; color: #999; border: 1px solid rgba(0,0,0,0.08);
  padding: 4px 10px; letter-spacing: 0.06em; text-transform: uppercase;
}
.srv-features { display: flex; flex-direction: column; justify-content: center; gap: 18px; }
.srv-feat { display: flex; align-items: flex-start; gap: 12px; }
.srv-feat-dot { width: 4px; height: 4px; background: #F96D00; border-radius: 50%; margin-top: 7px; flex-shrink: 0; }
.srv-feat-text { font-size: 0.82rem; color: #666; line-height: 1.6; }
.srv-feat-title { font-size: 0.68rem; color: #28282c; letter-spacing: 0.06em; text-transform: uppercase; margin-bottom: 3px; }

/* ── SOLUTIONS GRID ── */
.sol-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1px; background: rgba(0,0,0,0.06); }
.sol-card { background: #fff; padding: 32px; transition: background 0.2s; }
.sol-card:hover { background: #fafafa; }
.sol-card.span2 { grid-column: span 2; }
.sol-badge {
  font-size: 0.58rem; color: #F96D00; border: 1px solid rgba(249,109,0,0.3);
  padding: 3px 8px; letter-spacing: 0.1em; text-transform: uppercase;
  display: inline-block; margin-bottom: 14px;
}
.sol-name { font-size: 1.4rem; font-weight: 700; color: #28282c; line-height: 1; margin-bottom: 4px; }
.sol-subtitle { font-size: 0.62rem; color: #999; letter-spacing: 0.08em; text-transform: uppercase; margin-bottom: 14px; }
.sol-desc { font-size: 0.78rem; color: #666; line-height: 1.7; margin-bottom: 16px; }
.sol-features { display: flex; flex-direction: column; gap: 5px; }
.sol-feat { font-size: 0.72rem; color: #999; padding-left: 14px; position: relative; }
.sol-feat::before { content: '\2014'; position: absolute; left: 0; color: #F96D00; font-size: 0.68rem; }
```

- [ ] **Step 4: Write Marquee + Partners + CTA + FAQ + Responsive styles**

Append:

```css
/* ── MARQUEE ── */
#idx-marquee {
  padding: 32px 0; border-top: 1px solid rgba(0,0,0,0.06);
  border-bottom: 1px solid rgba(0,0,0,0.06); overflow: hidden;
}
.marquee-inner { display: inline-flex; animation: idxTicker 22s linear infinite; white-space: nowrap; }
.marquee-item {
  font-size: 3.6rem; font-weight: 700; color: transparent;
  -webkit-text-stroke: 0.5px rgba(0,0,0,0.06);
  letter-spacing: 0.03em; padding: 0 40px; flex-shrink: 0;
}
.marquee-item.fill { -webkit-text-stroke: none; color: #F96D00; }

/* ── PARTNERS ── */
#idx-partners { padding: 60px 48px; border-top: 1px solid rgba(0,0,0,0.06); }
.partners-label { font-size: 0.68rem; letter-spacing: 0.15em; color: #999; text-transform: uppercase; margin-bottom: 36px; }
.partners-row { display: flex; gap: 0; align-items: stretch; background: rgba(0,0,0,0.04); }
.partner-cell {
  flex: 1; padding: 28px 24px;
  border-right: 1px solid rgba(0,0,0,0.06);
  transition: background 0.2s;
}
.partner-cell:last-child { border-right: none; }
.partner-cell:hover { background: #fafafa; }
.partner-name { font-size: 1rem; font-weight: 600; color: #999; margin-bottom: 4px; transition: color 0.2s; }
.partner-cell:hover .partner-name { color: #28282c; }
.partner-type { font-size: 0.6rem; color: rgba(0,0,0,0.25); letter-spacing: 0.08em; text-transform: uppercase; }

/* ── CTA ── */
#idx-cta {
  padding: 80px 48px; position: relative; overflow: hidden;
  background: linear-gradient(135deg, #F96D00 0%, #e06000 50%, #c85500 100%);
  color: #fff;
}
#idx-cta::before {
  content: ''; position: absolute; inset: 0; pointer-events: none; opacity: 0.15; mix-blend-mode: soft-light;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
  background-size: 200px 200px;
}
.cta-eyebrow { font-size: 0.68rem; letter-spacing: 0.15em; color: rgba(255,255,255,0.5); text-transform: uppercase; margin-bottom: 16px; position: relative; z-index: 1; }
.cta-title { font-size: clamp(2rem, 4vw, 3.2rem); font-weight: 700; color: #fff; line-height: 1.1; margin-bottom: 12px; position: relative; z-index: 1; }
.cta-sub { font-size: 0.88rem; color: rgba(255,255,255,0.7); margin-bottom: 32px; position: relative; z-index: 1; }
.cta-actions { display: flex; gap: 20px; align-items: center; position: relative; z-index: 1; flex-wrap: wrap; }
.cta-btn {
  font-size: 0.78rem; letter-spacing: 0.1em; text-transform: uppercase;
  color: #F96D00; border: 1px solid rgba(255,255,255,0.5);
  background: #fff; padding: 14px 36px; text-decoration: none; display: inline-block;
  transition: background 0.2s, color 0.2s;
}
.cta-btn:hover { background: #28282c; color: #fff; border-color: #28282c; text-decoration: none; }
.cta-info { display: flex; flex-direction: column; gap: 3px; }
.cta-contact { font-size: 0.72rem; color: rgba(255,255,255,0.6); letter-spacing: 0.04em; }

/* ── FAQ ── */
#idx-faq { padding: 80px 48px; }
.faq-title { font-size: 1.6rem; font-weight: 700; color: #28282c; margin-bottom: 32px; }
.faq-item { border-bottom: 1px solid rgba(0,0,0,0.06); }
.faq-question {
  display: flex; justify-content: space-between; align-items: center;
  padding: 20px 0; cursor: pointer; transition: color 0.2s;
  font-size: 0.95rem; font-weight: 500; color: #28282c;
}
.faq-question:hover { color: #F96D00; }
.faq-question i { font-size: 0.8rem; color: #999; transition: transform 0.3s; }
.faq-item.open .faq-question i { transform: rotate(180deg); }
.faq-answer {
  max-height: 0; overflow: hidden; transition: max-height 0.3s ease;
  font-size: 0.85rem; color: #666; line-height: 1.8;
}
.faq-item.open .faq-answer { max-height: 300px; }
.faq-answer-inner { padding: 0 0 20px 0; }

/* ── RESPONSIVE ── */
@media (max-width: 992px) {
  #entryOverlay { left: 0; }
  #idx-hero { grid-template-columns: 1fr; padding: 48px 24px; }
  .hero-right { border-left: none; padding-left: 0; margin-top: 32px; }
  .idx-section { padding: 48px 24px; }
  .srv-grid { grid-template-columns: 1fr; }
  .srv-card.featured { grid-template-columns: 1fr; }
  .sol-grid { grid-template-columns: 1fr; }
  .sol-card.span2 { grid-column: span 1; }
  .partners-row { flex-wrap: wrap; }
  .partner-cell { flex: 0 0 50%; border-bottom: 1px solid rgba(0,0,0,0.06); }
  #idx-cta { padding: 48px 24px; }
  #idx-faq { padding: 48px 24px; }
  #idx-partners { padding: 36px 24px; }
  .entry-retrigger { bottom: 80px; right: 16px; }
}

@media (max-width: 576px) {
  .hero-sub-grid { grid-template-columns: 1fr; }
  .q-opts { grid-template-columns: 1fr; }
  .partner-cell { flex: 0 0 100%; }
}
```

- [ ] **Step 5: Verify styles load — open page in browser, confirm no broken layout**

```bash
# Refresh http://localhost:8000/index.html
# Verify: no CSS parse errors in console, page still loads
```

- [ ] **Step 6: Commit**

```bash
git add index.html
git commit -m "style(index): add all scoped CSS for new layout and entry overlay"
```

---

## Task 3: Build Entry Overlay HTML and JavaScript

**Files:**
- Modify: `index.html` (entry overlay section + inline `<script>` before closing `</body>`)

- [ ] **Step 1: Write Entry Overlay HTML**

Replace the `<!-- filled in Task 3 -->` placeholder inside `#entryOverlay` with:

```html
<button class="entry-skip" onclick="skipEntry()">略過 <i class="ion-ios-arrow-forward"></i></button>

<!-- Boot -->
<div id="bootScreen">
  <div class="boot-logo">AURO<span>BASE</span></div>
  <div class="boot-tagline">數位整合行銷 &middot; AI 智慧解決方案</div>
  <div class="boot-lines" id="bootLines"></div>
  <div class="boot-bar-wrap"><div class="boot-bar" id="bootBar"></div></div>
  <div class="boot-pct" id="bootPct">0%</div>
  <button class="boot-cta" id="bootCta" onclick="startQuiz()">開始診斷您的需求 <i class="ion-ios-arrow-forward"></i></button>
</div>

<!-- Quiz -->
<div id="quizScreen">
  <div class="q-progress" id="qProgress">
    <div class="q-dot active" id="qd0"></div>
    <div class="q-dot" id="qd1"></div>
    <div class="q-dot" id="qd2"></div>
  </div>
  <div class="q-step" id="qStep"></div>
  <div class="q-title" id="qTitle"></div>
  <div class="q-sub" id="qSub"></div>
  <div class="q-opts" id="qOpts"></div>
  <button class="q-next" id="qNext" onclick="nextQ()"></button>
</div>

<!-- Result Map -->
<div id="mapScreen">
  <div class="map-eyebrow">// 為您推薦的解決方案</div>
  <div class="map-title" id="mapTitle">您的專屬推薦</div>
  <div id="mapTerritories"></div>
  <div class="map-actions">
    <button class="map-enter" id="mapEnter" onclick="goRecommended()">前往推薦頁面 <i class="ion-ios-arrow-forward"></i></button>
    <button class="map-reset" onclick="resetQuiz()">重新診斷</button>
    <button class="map-home" onclick="skipEntry()">進入首頁</button>
  </div>
</div>
```

- [ ] **Step 2: Write Entry Overlay JavaScript**

Add a `<script>` block just before the existing `<script src="js/jquery.min.js">` line (before all external scripts, around line 1119 in original). This ensures the entry logic runs independently of jQuery.

```html
<script>
// ══ ENTRY OVERLAY ══

// -- localStorage check --
(function() {
  if (localStorage.getItem('aurobase_entry_done') === 'true') {
    var ov = document.getElementById('entryOverlay');
    if (ov) { ov.classList.add('closed'); }
    var rb = document.getElementById('entryRetrigger');
    if (rb) { rb.style.display = 'block'; }
  }
})();

// -- Boot sequence --
var bootMessages = [
  { text: '系統初始化...', delay: 200 },
  { text: '載入數位行銷模組', delay: 600 },
  { text: '連接 MarTech 平台', delay: 1000 },
  { text: '啟動 AI 分析引擎', delay: 1400 },
  { text: '版圖資料庫就緒', delay: 1800 },
  { text: '準備診斷系統', delay: 2200 }
];

var bootLines = document.getElementById('bootLines');
var bootBar = document.getElementById('bootBar');
var bootPct = document.getElementById('bootPct');
var bootCta = document.getElementById('bootCta');

if (bootLines && !document.getElementById('entryOverlay').classList.contains('closed')) {
  bootMessages.forEach(function(m, i) {
    var el = document.createElement('div');
    el.className = 'boot-line';
    el.innerHTML = '<em>//</em> ' + m.text;
    bootLines.appendChild(el);
    setTimeout(function() {
      el.classList.add('visible');
      var pct = Math.round(((i + 1) / bootMessages.length) * 100);
      bootBar.style.width = pct + '%';
      bootPct.textContent = pct + '%';
      if (i > 0) bootLines.children[i - 1].classList.add('done');
      if (i === bootMessages.length - 1) {
        setTimeout(function() { bootCta.classList.add('ready'); }, 300);
      }
    }, m.delay);
  });
}

// -- Quiz --
var questions = [
  {
    step: '// 01 — 核心挑戰', title: '您目前最大的挑戰是？',
    sub: '根據您的選擇，我們將為您推薦最相關的解決方案',
    opts: [
      { code: '01', label: '廣告效益', desc: '投放成本高，難以精準觸及目標客群', key: 'ads' },
      { code: '02', label: '客戶數據', desc: '數據分散，無法建立完整客戶視圖', key: 'data' },
      { code: '03', label: '服務效率', desc: '客服量大，人力成本居高不下', key: 'service' },
      { code: '04', label: 'AI 應用', desc: '想導入 AI，但不知道從何開始', key: 'ai' }
    ]
  },
  {
    step: '// 02 — 企業規模', title: '您的組織規模？',
    sub: '幫助我們推薦最適合的方案規格與導入深度',
    opts: [
      { code: 'A', label: '中型企業', desc: '100 人以下，快速成長中', key: 'sme' },
      { code: 'B', label: '大型企業', desc: '500 人以上，流程較為複雜', key: 'enterprise' },
      { code: 'C', label: '集團組織', desc: '多品牌或跨國經營需求', key: 'group' },
      { code: 'D', label: '新創公司', desc: '快速驗證市場，需靈活方案', key: 'startup' }
    ]
  },
  {
    step: '// 03 — 成效期望', title: '您希望多快看到成效？',
    sub: '這將決定我們為您規劃的導入策略與節奏',
    opts: [
      { code: 'I', label: '立即見效', desc: '一個月內，優先廣告優化與流量提升', key: 'fast' },
      { code: 'II', label: '中期布局', desc: '三至六個月，系統性建立數位能力', key: 'mid' },
      { code: 'III', label: '長期戰略', desc: '一年以上，打造完整數位生態系', key: 'long' },
      { code: 'IV', label: '尚在評估', desc: '希望先了解各方案後再決定', key: 'explore' }
    ]
  }
];

// Redirect map: Q1 answer -> primary page
var redirectMap = {
  ads: 'marketing.html',
  data: 'solution.html',
  service: 'contact.html',
  ai: 'ai-companion.html'
};

// Territory display data
var territoryData = {
  ads:     { code: '主力服務', name: '數位行銷', sub: 'Google · Facebook · SEO · 社群', tag: 'CORE', href: 'marketing.html' },
  data:    { code: '企業解決方案', name: 'MarTech', sub: 'CDP · CXA · Unica · CMS', tag: 'PLATFORM', href: 'solution.html' },
  service: { code: '客戶服務', name: '客服中心', sub: 'CTI · CRM · 智能客服', tag: 'SERVICE', href: 'contact.html' },
  ai:      { code: '創新服務', name: 'AI 智慧語伴', sub: '語音互動 · 健康守護', tag: 'AI', href: 'ai-companion.html' }
};

// Secondary recommendations based on Q2/Q3
var secondaryMap = {
  sme: 'ads', enterprise: 'data', group: 'service', startup: 'ai',
  fast: 'ads', mid: 'data', long: 'ai', explore: 'data'
};

var curQ = 0, answers = [], sel = null;

function startQuiz() {
  document.getElementById('bootScreen').style.display = 'none';
  document.getElementById('quizScreen').classList.add('active');
  renderQ();
}

function renderQ() {
  var q = questions[curQ];
  document.getElementById('qStep').textContent = q.step;
  document.getElementById('qTitle').textContent = q.title;
  document.getElementById('qSub').textContent = q.sub;
  document.getElementById('qOpts').innerHTML = q.opts.map(function(o) {
    return '<div class="q-opt" onclick="pickOpt(\'' + o.key + '\',this)">' +
      '<div class="q-opt-code">' + o.code + '</div>' +
      '<div class="q-opt-label">' + o.label + '</div>' +
      '<div class="q-opt-desc">' + o.desc + '</div></div>';
  }).join('');
  for (var i = 0; i < 3; i++) {
    document.getElementById('qd' + i).className = 'q-dot' + (i < curQ ? ' done' : i === curQ ? ' active' : '');
  }
  var btn = document.getElementById('qNext');
  btn.textContent = curQ < 2 ? '下一題' : '查看推薦方案';
  btn.classList.remove('ready');
  sel = null;
}

function pickOpt(key, el) {
  var opts = document.querySelectorAll('.q-opt');
  for (var i = 0; i < opts.length; i++) opts[i].classList.remove('selected');
  el.classList.add('selected');
  sel = key;
  document.getElementById('qNext').classList.add('ready');
}

function nextQ() {
  if (!sel) return;
  answers.push(sel);
  if (curQ < 2) { curQ++; renderQ(); }
  else showMap();
}

function showMap() {
  document.getElementById('quizScreen').classList.remove('active');
  var ms = document.getElementById('mapScreen');
  ms.classList.add('active');

  // Primary = Q1, secondary from Q2/Q3
  var primary = answers[0];
  var secKey2 = secondaryMap[answers[1]];
  var secKey3 = secondaryMap[answers[2]];
  var secondary = (secKey2 !== primary) ? secKey2 : (secKey3 !== primary ? secKey3 : null);

  var container = document.getElementById('mapTerritories');
  container.innerHTML = '';

  // Show primary
  container.appendChild(buildTerritory(primary, true));
  // Show secondary if different
  if (secondary && secondary !== primary) {
    container.appendChild(buildTerritory(secondary, false));
  }

  // Store primary redirect
  document.getElementById('mapEnter').setAttribute('data-href', redirectMap[primary]);
}

function buildTerritory(key, isRec) {
  var d = territoryData[key];
  var div = document.createElement('div');
  div.className = 'territory' + (isRec ? ' recommended' : '');
  div.innerHTML =
    '<div class="rec-badge">' + (isRec ? '推薦' : '') + '</div>' +
    '<div class="t-flex"><div>' +
    '<div class="t-code">' + d.code + '</div>' +
    '<div class="t-name">' + d.name + '</div>' +
    '<div class="t-sub">' + d.sub + '</div>' +
    '</div><div class="t-tag">' + d.tag + '</div></div>';
  div.style.cursor = 'pointer';
  div.onclick = function() { window.location.href = d.href; };
  return div;
}

function goRecommended() {
  var href = document.getElementById('mapEnter').getAttribute('data-href');
  if (href) {
    localStorage.setItem('aurobase_entry_done', 'true');
    window.location.href = href;
  }
}

function skipEntry() {
  var ov = document.getElementById('entryOverlay');
  ov.classList.add('closing');
  localStorage.setItem('aurobase_entry_done', 'true');
  setTimeout(function() {
    ov.classList.add('closed');
    var rb = document.getElementById('entryRetrigger');
    if (rb) rb.style.display = 'block';
  }, 650);
}

function openEntryOverlay() {
  var ov = document.getElementById('entryOverlay');
  ov.classList.remove('closing', 'closed');
  document.getElementById('entryRetrigger').style.display = 'none';
  // Reset to boot
  resetQuiz();
}

function resetQuiz() {
  curQ = 0; answers = []; sel = null;
  document.getElementById('mapScreen').classList.remove('active');
  document.getElementById('quizScreen').classList.remove('active');
  document.getElementById('bootScreen').style.display = 'flex';
  // Re-run boot animation
  bootLines.innerHTML = '';
  bootBar.style.width = '0%';
  bootPct.textContent = '0%';
  bootCta.classList.remove('ready');
  bootMessages.forEach(function(m, i) {
    var el = document.createElement('div');
    el.className = 'boot-line';
    el.innerHTML = '<em>//</em> ' + m.text;
    bootLines.appendChild(el);
    setTimeout(function() {
      el.classList.add('visible');
      var pct = Math.round(((i + 1) / bootMessages.length) * 100);
      bootBar.style.width = pct + '%';
      bootPct.textContent = pct + '%';
      if (i > 0) bootLines.children[i - 1].classList.add('done');
      if (i === bootMessages.length - 1) {
        setTimeout(function() { bootCta.classList.add('ready'); }, 300);
      }
    }, m.delay);
  });
}
</script>
```

- [ ] **Step 3: Verify overlay works end-to-end in browser**

```
1. Clear localStorage: localStorage.removeItem('aurobase_entry_done')
2. Refresh page — overlay should appear with boot animation
3. Click "開始診斷您的需求" — quiz appears
4. Answer all 3 questions — result map appears
5. Click "前往推薦頁面" — redirects to correct page
6. Go back to index.html — overlay should NOT appear (localStorage set)
7. Click "需求診斷" retrigger button — overlay reopens
8. Click "略過" — overlay fades, homepage shows
```

- [ ] **Step 4: Commit**

```bash
git add index.html
git commit -m "feat(index): add interactive entry overlay with need-assessment quiz"
```

---

## Task 4: Build Hero and Ticker sections

**Files:**
- Modify: `index.html` (`#idx-hero` and `#idx-ticker` sections)

- [ ] **Step 1: Write Hero HTML**

Replace `<section id="idx-hero"><!-- filled in Task 4 --></section>` with:

```html
<section id="idx-hero">
  <div class="hero-left">
    <div class="hero-eyebrow">您最專業的科技行銷顧問</div>
    <h1 class="hero-title">
      數位整合<br>行銷<br><span class="orange">解決方案</span>
    </h1>
    <p class="hero-desc">結合數位廣告、行銷自動化、AI 智慧技術，協助企業精準觸及目標客群，從數據洞察到客戶體驗，全面提升品牌競爭力。</p>
    <div class="hero-actions">
      <a href="contact.html" class="btn-primary-idx">填寫諮詢表單</a>
      <a href="#idx-services" class="btn-ghost-idx">了解服務 <i class="ion-ios-arrow-forward"></i></a>
    </div>
  </div>

  <div class="hero-right">
    <div class="hero-map">
      <div class="hero-map-label">// 核心服務版圖</div>

      <a href="marketing.html" class="hero-svc">
        <div class="hero-svc-row">
          <div>
            <div class="hero-svc-code">主力服務</div>
            <div class="hero-svc-name">數位行銷</div>
            <div class="hero-svc-sub">Google · Facebook · SEO · 社群經營</div>
          </div>
          <div class="hero-svc-tag">CORE</div>
        </div>
      </a>

      <div class="hero-sub-grid">
        <a href="solution.html" class="hero-svc">
          <div class="hero-svc-code">企業解決方案</div>
          <div class="hero-svc-name">MarTech</div>
          <div class="hero-svc-sub">CDP · CXA · Unica · CMS</div>
        </a>
        <a href="contact.html" class="hero-svc">
          <div class="hero-svc-code">客戶服務</div>
          <div class="hero-svc-name">客服中心</div>
          <div class="hero-svc-sub">CTI · CRM · 智能客服</div>
        </a>
        <a href="ai-companion.html" class="hero-svc">
          <div class="hero-svc-code">創新服務</div>
          <div class="hero-svc-name">AI 語伴</div>
          <div class="hero-svc-sub">語音互動 · 健康守護</div>
        </a>
        <a href="contact.html" class="hero-svc">
          <div class="hero-svc-code">開始合作</div>
          <div class="hero-svc-name">聯絡我們</div>
          <div class="hero-svc-sub">量身規劃專屬方案</div>
        </a>
      </div>
    </div>
  </div>
</section>
```

- [ ] **Step 2: Write Ticker HTML**

Replace `<div id="idx-ticker"><!-- filled in Task 4 --></div>` with:

```html
<div id="idx-ticker">
  <div class="ticker-inner">
    <span class="ticker-item"><span>◈</span> 數位廣告投放</span>
    <span class="ticker-item"><span>⊞</span> 行銷自動化</span>
    <span class="ticker-item"><span>⬡</span> 顧客數據平台 CDP</span>
    <span class="ticker-item"><span>◉</span> AI 智慧語伴</span>
    <span class="ticker-item"><span>⇌</span> 客服中心系統</span>
    <span class="ticker-item"><span>◈</span> SEO 優化</span>
    <span class="ticker-item"><span>⊞</span> HCL Unica</span>
    <span class="ticker-item"><span>⬡</span> Magnolia CMS</span>
    <span class="ticker-item"><span>◉</span> Acoustic CXA</span>
    <span class="ticker-item"><span>⇌</span> 社群經營</span>
    <span class="ticker-item"><span>◈</span> 數位廣告投放</span>
    <span class="ticker-item"><span>⊞</span> 行銷自動化</span>
    <span class="ticker-item"><span>⬡</span> 顧客數據平台 CDP</span>
    <span class="ticker-item"><span>◉</span> AI 智慧語伴</span>
    <span class="ticker-item"><span>⇌</span> 客服中心系統</span>
    <span class="ticker-item"><span>◈</span> SEO 優化</span>
    <span class="ticker-item"><span>⊞</span> HCL Unica</span>
    <span class="ticker-item"><span>⬡</span> Magnolia CMS</span>
    <span class="ticker-item"><span>◉</span> Acoustic CXA</span>
    <span class="ticker-item"><span>⇌</span> 社群經營</span>
  </div>
</div>
```

- [ ] **Step 3: Verify in browser**

```
1. Skip entry overlay (click 略過)
2. Hero: two-column layout visible, service cards on right are clickable
3. Ticker: scrolling horizontally with orange symbols
4. Test hero card hover: orange left border should animate in
```

- [ ] **Step 4: Commit**

```bash
git add index.html
git commit -m "feat(index): add hero section and ticker marquee"
```

---

## Task 5: Build Core Services and Solutions sections

**Files:**
- Modify: `index.html` (`#idx-services` and `#idx-solutions` sections)

- [ ] **Step 1: Write Core Services HTML**

Replace `<section id="idx-services"><!-- filled in Task 5 --></section>` with:

```html
<section id="idx-services" class="idx-section">
  <div class="idx-section-header">
    <div>
      <div class="idx-section-num">// 01 — 核心服務</div>
      <h2 class="idx-section-title">全方位數位<br>行銷解決方案</h2>
    </div>
    <a href="marketing.html" class="idx-section-link">了解更多 <i class="ion-ios-arrow-forward"></i></a>
  </div>

  <div class="srv-grid">
    <div class="srv-card featured">
      <div>
        <div class="srv-idx">// 主力服務</div>
        <div class="srv-name">數位行銷</div>
        <div class="srv-subtitle">Digital Advertising · SRM · Automated Marketing</div>
        <p class="srv-desc">提供全方位數位行銷解決方案，從 Google 關鍵字廣告、Facebook 廣告投放到社群經營、SEO 優化，協助企業精準觸及目標客群，提升品牌曝光與轉換率。</p>
        <div class="srv-tags">
          <span class="srv-tag">Google Ads</span>
          <span class="srv-tag">Facebook Ads</span>
          <span class="srv-tag">SEO 優化</span>
          <span class="srv-tag">社群經營</span>
          <span class="srv-tag">數據分析</span>
        </div>
      </div>
      <div class="srv-features">
        <div class="srv-feat">
          <div class="srv-feat-dot"></div>
          <div><div class="srv-feat-title">Collisions 碰撞思維</div><div class="srv-feat-text">鼓勵思想互相碰撞，激發更多不同面向的想法與策略。</div></div>
        </div>
        <div class="srv-feat">
          <div class="srv-feat-dot"></div>
          <div><div class="srv-feat-title">Communication 有效溝通</div><div class="srv-feat-text">專案執行前進行有效溝通，過程中不斷調整，迅速找出最佳解決方案。</div></div>
        </div>
        <div class="srv-feat">
          <div class="srv-feat-dot"></div>
          <div><div class="srv-feat-title">Collaboration 專業協作</div><div class="srv-feat-text">Google 合作夥伴，具備電商、數據分析、機器學習豐富經驗。</div></div>
        </div>
      </div>
    </div>

    <div class="srv-card">
      <div class="srv-idx">// 客戶服務</div>
      <div class="srv-name">客服中心</div>
      <div class="srv-subtitle">CTI · CRM · 智能客服建置</div>
      <p class="srv-desc">專業客服中心系統建置與外包服務，整合 CTI 系統、CRM、智能客服機器人，提供完整客戶服務解決方案，提升滿意度與服務效率。</p>
      <div class="srv-tags">
        <span class="srv-tag">CTI 系統</span>
        <span class="srv-tag">CRM 整合</span>
        <span class="srv-tag">智能機器人</span>
        <span class="srv-tag">系統外包</span>
      </div>
    </div>

    <div class="srv-card">
      <div class="srv-idx">// 創新服務</div>
      <div class="srv-name">AI 智慧語伴</div>
      <div class="srv-subtitle">智慧陪伴 · 健康守護新世代</div>
      <p class="srv-desc">以 AI 語音為核心，打造智慧陪伴與健康守護。結合語音互動與智慧分析，主動陪伴長者、即時掌握健康與情緒變化，協助社工快速掌握狀況。</p>
      <div class="srv-tags">
        <span class="srv-tag">AI 語音</span>
        <span class="srv-tag">情感互動</span>
        <span class="srv-tag">健康監測</span>
        <span class="srv-tag">關懷報告</span>
      </div>
    </div>
  </div>
</section>
```

- [ ] **Step 2: Write Solutions Grid HTML**

Replace `<section id="idx-solutions"><!-- filled in Task 5 --></section>` with:

```html
<section id="idx-solutions" class="idx-section" style="padding-top:0;">
  <div class="idx-section-header">
    <div>
      <div class="idx-section-num">// 02 — 解決方案 & 產品</div>
      <h2 class="idx-section-title">企業級<br>MarTech 平台</h2>
    </div>
    <a href="solution.html" class="idx-section-link">查看全部 <i class="ion-ios-arrow-forward"></i></a>
  </div>

  <div class="sol-grid">
    <div class="sol-card">
      <div class="sol-badge">顧客體驗</div>
      <div class="sol-name">Acoustic CXA</div>
      <div class="sol-subtitle">顧客體驗分析平台</div>
      <p class="sol-desc">深入了解客戶行為，優化使用者體驗。透過先進分析工具，掌握客戶旅程中的每個接觸點。</p>
      <div class="sol-features">
        <div class="sol-feat">即時行為分析與追蹤</div>
        <div class="sol-feat">客戶旅程視覺化</div>
        <div class="sol-feat">個人化內容推薦</div>
        <div class="sol-feat">A/B 測試與優化</div>
      </div>
    </div>

    <div class="sol-card">
      <div class="sol-badge">數據整合</div>
      <div class="sol-name">CDP</div>
      <div class="sol-subtitle">顧客數據平台</div>
      <p class="sol-desc">整合多渠道顧客數據，建立完整的 360 度客戶視圖。運用 AI 技術進行數據分析，實現精準行銷。</p>
      <div class="sol-features">
        <div class="sol-feat">多源數據整合</div>
        <div class="sol-feat">統一客戶檔案管理</div>
        <div class="sol-feat">AI 智能分群與預測</div>
        <div class="sol-feat">即時數據同步</div>
      </div>
    </div>

    <div class="sol-card">
      <div class="sol-badge">行銷自動化</div>
      <div class="sol-name">HCL Unica</div>
      <div class="sol-subtitle">企業級行銷自動化</div>
      <p class="sol-desc">企業級行銷自動化解決方案，整合多渠道行銷活動，實現精準的客戶溝通與互動管理。</p>
      <div class="sol-features">
        <div class="sol-feat">跨渠道行銷編排</div>
        <div class="sol-feat">智能化活動管理</div>
        <div class="sol-feat">個人化內容投放</div>
        <div class="sol-feat">效果追蹤與分析</div>
      </div>
    </div>

    <div class="sol-card">
      <div class="sol-badge">內容管理</div>
      <div class="sol-name">Magnolia CMS</div>
      <div class="sol-subtitle">企業內容管理系統</div>
      <p class="sol-desc">強大的企業級內容管理系統，提供靈活的內容創建與管理能力，支援多語言、多網站管理。</p>
      <div class="sol-features">
        <div class="sol-feat">直覺式內容編輯</div>
        <div class="sol-feat">多語言多網站支援</div>
        <div class="sol-feat">RESTful API 整合</div>
        <div class="sol-feat">Headless CMS 架構</div>
      </div>
    </div>

    <div class="sol-card span2">
      <div class="sol-badge">智慧照護</div>
      <div class="sol-name">AI 智慧語伴</div>
      <div class="sol-subtitle">創新研發 · 智慧陪伴新世代</div>
      <p class="sol-desc">以 AI 語音為核心，打造智慧陪伴與健康守護平台。結合語音互動與智慧分析，主動陪伴長者、即時掌握健康與情緒變化，協助社工快速掌握狀況，用科技實踐關懷。</p>
      <div class="sol-features" style="flex-direction:row;flex-wrap:wrap;gap:8px;">
        <div class="sol-feat">AI 語音情感互動</div>
        <div class="sol-feat">健康與情緒監測</div>
        <div class="sol-feat">自動關懷報告生成</div>
        <div class="sol-feat">用藥提醒與語音回報</div>
      </div>
    </div>
  </div>
</section>
```

- [ ] **Step 3: Verify in browser**

```
1. Core Services: featured card spans full width with 2 columns, 2 half-width cards below
2. Solutions: 3-column grid, AI card spans 2 columns
3. All cards hover -> slight gray background
4. Section links point to correct pages
```

- [ ] **Step 4: Commit**

```bash
git add index.html
git commit -m "feat(index): add core services and solutions grid sections"
```

---

## Task 6: Build Marquee, Partners, and CTA sections

**Files:**
- Modify: `index.html` (`#idx-marquee`, `#idx-partners`, `#idx-cta` sections)

- [ ] **Step 1: Write Marquee HTML**

Replace `<div id="idx-marquee"><!-- filled in Task 6 --></div>` with:

```html
<div id="idx-marquee">
  <div class="marquee-inner">
    <span class="marquee-item">DIGITAL MARKETING</span>
    <span class="marquee-item fill">AUROBASE</span>
    <span class="marquee-item">數位整合行銷</span>
    <span class="marquee-item fill">AI 智慧</span>
    <span class="marquee-item">MARTECH</span>
    <span class="marquee-item fill">台灣</span>
    <span class="marquee-item">DIGITAL MARKETING</span>
    <span class="marquee-item fill">AUROBASE</span>
    <span class="marquee-item">數位整合行銷</span>
    <span class="marquee-item fill">AI 智慧</span>
    <span class="marquee-item">MARTECH</span>
    <span class="marquee-item fill">台灣</span>
  </div>
</div>
```

- [ ] **Step 2: Write Partners HTML**

Replace `<div id="idx-partners"><!-- filled in Task 6 --></div>` with:

```html
<div id="idx-partners">
  <div class="partners-label">// 事業夥伴</div>
  <div class="partners-row">
    <div class="partner-cell">
      <div class="partner-name">Magnolia</div>
      <div class="partner-type">CMS 企業夥伴</div>
    </div>
    <div class="partner-cell">
      <div class="partner-name">HCL Software</div>
      <div class="partner-type">行銷自動化夥伴</div>
    </div>
    <div class="partner-cell">
      <div class="partner-name">朋昶數位</div>
      <div class="partner-type">數位科技夥伴</div>
    </div>
    <div class="partner-cell">
      <div class="partner-name">Acoustic</div>
      <div class="partner-type">顧客體驗夥伴</div>
    </div>
    <div class="partner-cell">
      <div class="partner-name">Google</div>
      <div class="partner-type">廣告認證夥伴</div>
    </div>
  </div>
</div>
```

- [ ] **Step 3: Write CTA HTML**

Replace `<section id="idx-cta"><!-- filled in Task 6 --></section>` with:

```html
<section id="idx-cta">
  <div class="cta-eyebrow">// 開始合作</div>
  <h2 class="cta-title">歡迎填寫<br>諮詢表單</h2>
  <p class="cta-sub">由專人為您量身規劃最適合的數位行銷解決方案</p>
  <div class="cta-actions">
    <a href="contact.html" class="cta-btn">立即諮詢 <i class="ion-ios-arrow-forward"></i></a>
    <div class="cta-info">
      <div class="cta-contact">+886-2-5568-4688</div>
      <div class="cta-contact">ad@aurocore.com</div>
    </div>
  </div>
</section>
```

- [ ] **Step 4: Verify in browser**

```
1. Marquee: large outlined text scrolling, "AUROBASE" and "AI 智慧" in orange
2. Partners: 5 cells in a row, text gray -> dark on hover
3. CTA: orange gradient background, white text, button and contact info visible
```

- [ ] **Step 5: Commit**

```bash
git add index.html
git commit -m "feat(index): add marquee, partners, and CTA sections"
```

---

## Task 7: Build FAQ section and final cleanup

**Files:**
- Modify: `index.html` (`#idx-faq` section)

- [ ] **Step 1: Write FAQ HTML**

Replace `<section id="idx-faq"><!-- filled in Task 7 --></section>` with:

```html
<section id="idx-faq">
  <h2 class="faq-title">常見問題</h2>

  <div class="faq-item" onclick="toggleFaq(this)">
    <div class="faq-question">
      奧洛提供哪些數位行銷服務？
      <i class="ion-ios-arrow-down"></i>
    </div>
    <div class="faq-answer">
      <div class="faq-answer-inner">奧洛提供數位廣告投放（Google/Facebook）、行銷自動化、CDP客戶數據平台、AI智慧語伴、客服中心系統建置與外包服務。</div>
    </div>
  </div>

  <div class="faq-item" onclick="toggleFaq(this)">
    <div class="faq-question">
      什麼是數位整合行銷？
      <i class="ion-ios-arrow-down"></i>
    </div>
    <div class="faq-answer">
      <div class="faq-answer-inner">數位整合行銷是整合SEO、社群媒體、廣告投放、內容行銷與數據分析的全方位行銷策略，幫助企業在數位環境中最大化品牌曝光與轉換率。</div>
    </div>
  </div>

  <div class="faq-item" onclick="toggleFaq(this)">
    <div class="faq-question">
      行銷自動化能為企業帶來什麼效益？
      <i class="ion-ios-arrow-down"></i>
    </div>
    <div class="faq-answer">
      <div class="faq-answer-inner">行銷自動化可提升潛在客戶轉換率、減少行銷人力成本，從潛客培育到客戶留存全面優化行銷流程。奧洛提供Acoustic CXA與HCL Unica等企業級行銷自動化解決方案。</div>
    </div>
  </div>
</section>
```

- [ ] **Step 2: Add FAQ toggle function to the entry overlay script block**

Append to the existing `<script>` block (the one added in Task 3):

```javascript
// ══ FAQ ACCORDION ══
function toggleFaq(el) {
  var isOpen = el.classList.contains('open');
  // Close all
  var items = document.querySelectorAll('.faq-item');
  for (var i = 0; i < items.length; i++) items[i].classList.remove('open');
  // Open clicked (if was closed)
  if (!isOpen) el.classList.add('open');
}
```

- [ ] **Step 3: Remove the old particles.js container and script reference**

The old `index.html` had `<div id="particles-js"></div>` inside `#colorlib-main` and loaded `particles.min.js`. Since the new design doesn't use particles:

1. Remove `<div id="particles-js"></div>` from inside `#colorlib-main` (if it still exists after Task 1 scaffolding)
2. Remove the `<script src="https://cdn.jsdelivr.net/particles.js/2.0.0/particles.min.js"></script>` line from the bottom of the file

- [ ] **Step 4: Delete the backup file**

```bash
rm index.html.bak
```

- [ ] **Step 5: Full end-to-end verification in browser**

```
Desktop (>992px):
1. First visit (clear localStorage) -> overlay appears with boot animation
2. Complete quiz -> redirects to correct page
3. Return to index -> overlay hidden, retrigger button visible
4. Click retrigger -> overlay reopens
5. Skip overlay -> all 9 sections visible and properly styled
6. Hero: two-column, cards clickable
7. Ticker: scrolling
8. Services: grid layout with featured card
9. Solutions: 3-column grid
10. Marquee: large text scrolling
11. Partners: 5 cells
12. CTA: orange gradient
13. FAQ: accordion open/close
14. Footer: present
15. Sidebar: fully functional, nav links work
16. Float button + modal: still working

Mobile (<768px):
17. Resize to mobile width
18. Overlay goes full width
19. Hero stacks vertically
20. Grids become single column
21. Partners wrap
```

- [ ] **Step 6: Commit**

```bash
git add index.html
git commit -m "feat(index): add FAQ section, remove particles.js, complete new homepage"
```

---

## Self-Review Checklist

- **Spec coverage**: All 9 sections (Hero, Ticker, Services, Solutions, Marquee, Partners, CTA, FAQ, Footer) covered. Entry overlay with 3 stages covered. localStorage logic covered. Re-trigger button covered. Responsive breakpoints covered. SEO preservation (head unchanged) covered. Inquiry modal (unchanged) covered.
- **Placeholder scan**: No TBD/TODO. All steps have complete code.
- **Type consistency**: CSS class names consistent across tasks (`.srv-card`, `.sol-card`, `.idx-section`, etc.). JS function names match HTML onclick attributes (`skipEntry`, `startQuiz`, `nextQ`, `pickOpt`, `goRecommended`, `resetQuiz`, `openEntryOverlay`, `toggleFaq`).
- **Missing from spec**: `客服中心` in hero links to `contact.html` rather than `https://aurocore.com/` — this matches the quiz redirect logic (service -> contact.html). The sidebar nav still links to `https://aurocore.com/` for 客服中心 which is correct (sidebar is unchanged).
